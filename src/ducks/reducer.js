let initialState = {

}

// ACTION CONSTANTS

// ACTION CREATORS

// REDUCER
export default function reducer(state=initialState, action) {
  switch(action.type) {
    default: 
      return state;
  }
}